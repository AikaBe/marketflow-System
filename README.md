    hello
     
    после клона заходим в **marketflow** и потом **docker** и только отсюда запускаем 

    Что бы все открылось сначало нужно написать вот так:

    chmod +x load_images.sh

    потом вот так:

    docker-compose up load_images

    и только потом так:
     
     docker-compose up